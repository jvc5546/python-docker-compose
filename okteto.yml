deploy:
  compose: docker-compose.yml
dev:
  api:
    command: bash
    sync:
    - '.:/app'
    volumes:
    - /root/.cache/pip
    forward:
    - 8080:8080
    reverse:
    - 9000:9000
    services:
    - name: dependency-app
